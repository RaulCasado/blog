---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
	title: string;
	description: string;
	lang?: string;
	pageType?: 'home' | 'article' | 'about' | 'page';
}

const { title, description, lang = 'es', pageType = 'page' } = Astro.props;
---

<html lang={lang}>
	<head>
		<BaseHead title={title} description={description} lang={lang} pageType={pageType} />
		<style>
			main {
				width: calc(100% - 2em);
				max-width: 1200px;
				margin: 0 auto;
				padding: 1rem;
			}
			.page-content {
				padding-top: 2rem;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div class="page-content">
				<slot />
			</div>
		</main>
		<Footer />
	</body>
</html>
